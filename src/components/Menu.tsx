import React from 'react'
import MenuButton from '../nav/MenuButton'

/**
 * Simple Menu component that pages can import
 * Contains all navigation logic internally
 */
export default function Menu() {
  return <MenuButton />
}
